(function () {
  'use strict';

  const ENDPOINT = '/api/weekly-highlights';
  const DEFAULTS = {
    highlight1: { imageUrl: '', title: '', description: '', published: false },
    highlight2: { imageUrl: '', title: '', description: '', published: false }
  };
  let injected = false;
  let current = null;

  const clone = (value) => JSON.parse(JSON.stringify(value));
  const validHttps = (value) => { try { return new URL(value).protocol === 'https:'; } catch { return false; } };
  function node(tag, classes, value) { const element = document.createElement(tag); if (classes) element.className = classes; if (value !== undefined) element.textContent = value; return element; }
  function inputField(label, item, key, type, onChange) { const wrapper = node('label', 'block'); wrapper.append(node('span', 'block text-sm font-semibold mb-1.5 text-sec', label)); const input = document.createElement(type === 'textarea' ? 'textarea' : 'input'); input.className = 'md-input'; if (type === 'textarea') input.rows = 3; else input.type = type; input.value = item[key] || ''; input.addEventListener('input', () => onChange(input.value)); wrapper.append(input); return wrapper; }
  function preview(item) { const box = node('div', 'mt-3 rounded-xl border border-theme overflow-hidden bg-surface-warm min-h-[160px] flex items-center justify-center'); if (validHttps(item.imageUrl)) { const image = document.createElement('img'); image.src = item.imageUrl; image.alt = item.title || 'Weekly highlight preview'; image.loading = 'lazy'; image.decoding = 'async'; image.className = 'w-full aspect-[4/3] object-cover'; image.addEventListener('error', () => box.replaceChildren(node('p', 'p-5 text-sm text-red text-center', 'Unable to load this image URL. Check the HTTPS URL.')), { once: true }); box.append(image); } else box.append(node('p', 'p-5 text-sm text-muted text-center', item.imageUrl ? 'Enter a valid HTTPS image URL to preview it.' : 'Image preview will appear here.')); return box; }
  function card(slot, item) { const wrapper = node('article', 'rounded-2xl border border-theme p-5 bg-theme/60'); wrapper.append(node('h3', 'font-bold text-lg text-heading', slot === 'highlight1' ? 'Highlight 1' : 'Highlight 2')); wrapper.append(inputField('Image HTTPS URL', item, 'imageUrl', 'url', (value) => { item.imageUrl = value; rerender(); })); wrapper.append(preview(item)); const fields = node('div', 'mt-4 space-y-4'); fields.append(inputField('Title (optional)', item, 'title', 'text', (value) => { item.title = value; })); fields.append(inputField('Description (optional)', item, 'description', 'textarea', (value) => { item.description = value; })); wrapper.append(fields); const publish = node('label', 'inline-flex items-center gap-2 text-sm font-semibold text-sec cursor-pointer mt-4'); const checkbox = document.createElement('input'); checkbox.type = 'checkbox'; checkbox.checked = item.published === true; checkbox.addEventListener('change', () => { item.published = checkbox.checked; }); publish.append(checkbox, node('span', '', 'Published')); wrapper.append(publish); return wrapper; }
  function showToast(message, bad) { const toast = node('div', 'fixed right-5 bottom-5 z-[140] max-w-sm rounded-xl px-4 py-3 text-sm font-semibold text-white shadow-xl', message); toast.style.background = bad ? '#b42318' : '#111827'; document.body.append(toast); window.setTimeout(() => toast.remove(), 3200); }
  function rerender() { const section = document.querySelector('[data-weekly-highlights-admin]'); if (!section || !current) return; const grid = section.querySelector('[data-weekly-grid]'); if (!grid) return; grid.replaceChildren(card('highlight1', current.highlight1), card('highlight2', current.highlight2)); }
  async function save() { const button = document.querySelector('[data-weekly-save]'); if (button) { button.disabled = true; button.textContent = 'Saving…'; } try { for (const item of [current.highlight1, current.highlight2]) { if (item.published && !validHttps(item.imageUrl)) throw new Error('Every published highlight needs a valid HTTPS image URL.'); if (item.imageUrl && !validHttps(item.imageUrl)) throw new Error('Image URLs must use HTTPS.'); } const response = await fetch(ENDPOINT, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ weeklyHighlights: current }), cache: 'no-store' }); const result = await response.json().catch(() => ({})); if (!response.ok) throw new Error(result.error || 'Unable to save Weekly Highlights.'); current = clone(result.weeklyHighlights); rerender(); showToast('Weekly Highlights saved successfully.'); } catch (error) { showToast(error.message || 'Unable to save Weekly Highlights.', true); } finally { if (button) { button.disabled = false; button.textContent = 'Save Weekly Highlights'; } } }
  async function load() { try { const response = await fetch(ENDPOINT, { cache: 'no-store' }); const result = await response.json().catch(() => ({})); if (!response.ok) throw new Error(result.error || 'Unable to load Weekly Highlights.'); current = clone(result.weeklyHighlights || DEFAULTS); } catch { current = clone(DEFAULTS); } render(); }
  function render() { if (injected || !current) return; const dashboard = document.querySelector('main'); if (!dashboard || !dashboard.querySelector('#saveAll')) return; const section = node('section', 'material-card p-5'); section.dataset.weeklyHighlightsAdmin = 'true'; const header = node('div', 'flex flex-col lg:flex-row lg:items-start lg:justify-between gap-4'); const info = node('div', 'min-w-0'); info.append(node('h2', 'text-xl md:text-2xl font-bold text-heading', '📸 Weekly Highlights'), node('p', 'text-sm font-medium text-gold mt-1', 'Where this changes: resources.html → Weekly Highlights'), node('p', 'text-xs text-muted mt-2', 'Manage exactly two weekly images. Published highlights are shown publicly.')); const saveButton = node('button', 'btn-primary text-sm', 'Save Weekly Highlights'); saveButton.type = 'button'; saveButton.dataset.weeklySave = 'true'; saveButton.addEventListener('click', save); header.append(info, saveButton); section.append(header); const grid = node('div', 'grid grid-cols-1 lg:grid-cols-2 gap-5 mt-5'); grid.dataset.weeklyGrid = 'true'; section.append(grid); const existing = Array.from(dashboard.children); const anchor = existing.find((child) => child.querySelector?.('h2')?.textContent?.includes('Featured Video')) || existing.find((child) => child.querySelector?.('h2')?.textContent?.includes('Resources')); if (anchor) dashboard.insertBefore(section, anchor); else dashboard.insertBefore(section, dashboard.children[1] || null); injected = true; rerender(); }
  function boot() { if (document.querySelector('[data-weekly-highlights-admin]')) return; const observer = new MutationObserver(() => { if (!injected) render(); if (injected) observer.disconnect(); }); observer.observe(document.body, { childList: true, subtree: true }); load(); }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot, { once: true }); else boot();
})();
